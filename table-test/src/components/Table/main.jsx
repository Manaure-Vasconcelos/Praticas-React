import React from 'react'
import ReactDOM from 'react-dom/client'
import Table from './tableContainer.jsx'
import '../../styles/global-style.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Table />
  </React.StrictMode>,
)
